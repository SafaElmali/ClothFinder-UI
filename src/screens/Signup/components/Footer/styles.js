import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    footerText: {
        color: '#d3d3d3',
    },
    signInText: {
        color: '#fff',
        fontWeight: 'bold'
    }
});

export default styles;